using UnityEngine;
using System.Collections;

public enum GameState {InitPhase,ChargingPhase,WaitingPhase,ShootPhase}

public class GameController : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
